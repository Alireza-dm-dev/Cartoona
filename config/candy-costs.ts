export interface CandyCosts {
  imageCreation: number;
  videoRequest: number;
  drawingAnimation: number;
}

/**
 * TODO: Finalize candy costs for each creation type.
 * These are placeholder values and will change before MVP launch.
 */
export const candyCosts: CandyCosts = {
  imageCreation: 0,
  videoRequest: 0,
  drawingAnimation: 0,
};
